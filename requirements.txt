# Pinned to the versions this project was developed and tested against.
# Regenerate deliberately, not automatically — pandas 3.x in particular
# changed enough that an unpinned install can break analyzer.py silently.
fastapi==0.141.1
uvicorn==0.52.4
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
scipy==1.18.1
beautifulsoup4==4.15.0
requests==2.34.2
python-multipart==0.0.32
